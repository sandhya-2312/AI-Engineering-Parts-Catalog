# Local development — Vite proxies /api to http://localhost:3000
VITE_API_URL=/api

# Production (optional — Vercel). If unset, production builds use the Render API automatically.
# VITE_API_URL=https://ai-engineering-parts-catalog-api.onrender.com/api
